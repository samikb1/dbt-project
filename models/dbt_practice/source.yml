version: 2

sources:
  - name: src_dept_raw
    database: TEST_POC
    schema: PUBLIC
    tables:
      - name: dept_emp
        columns:
          - name: EMP_NO
            meta:
              masking_policy: pii_number
          - name: DEPT_NO
            meta:
              masking_policy: pii_string